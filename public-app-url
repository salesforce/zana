# Public origin remotes use to curl /install.sh and enroll (Host-header allowlist).
# Product HTTP still binds loopback. This hostname is the Heroku pairing door
# (docs site + /install.sh). The laptop dials it outbound; do not inbound-proxy
# to the Mac. Precedence: env ZCC_APP_URL > Settings → Machines → Public app URL > this file.
https://zcc-7808c5bc8f3d.herokuapp.com
