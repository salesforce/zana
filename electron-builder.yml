# Compatibility shim — the real config lives with the desktop app.
# electron-builder still runs from the repo root (projectDir = this directory),
# so extraResources paths in the extended file stay repo-relative.
extends: apps/desktop/electron-builder.yml
