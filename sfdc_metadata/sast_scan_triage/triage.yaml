# SAST scan triage — APPLICATION-SECURITY-AGENT/SAST
#
# Entries here suppress specific SAST findings that have been reviewed and
# determined to be false positives or not exploitable. Each `id` is the
# `<path>:<triageId>` reported in the failing check's annotation.

false_positive:
  # Rule: base.genai.restricted-models.detect-chinese-llm-model-names (SFSS-SSD-004)
  # These two findings flag documentation comments describing the shared
  # `seedPromptArgs` seed-prompt delivery helper. The comments only enumerate the
  # supported coding-agent HARNESS FAMILIES (claude / cursor / codex / pi /
  # OpenCode) and the CLI flag shape (`--prompt` vs a positional arg). No
  # Chinese-developed AI model name is referenced anywhere in these files (verified
  # by full-file grep for deepseek/qwen/glm/kimi/chatglm/baichuan/moonshot/etc.);
  # the rule mis-fired on prose about launch-harness plumbing. No restricted model
  # is used, configured, or referenced.
  - id: src/renderer/components/AgentLauncher.tsx:a1850496108e365919ff906a3ad19aef03d419c820d362ae38782e0bd4fce0ad
    justification: >-
      False positive. This line is a doc comment for buildLaunchArgs describing the
      shared seedPromptArgs helper; it only names supported harness families
      (claude/cursor/codex/pi/OpenCode) and the CLI seed-prompt flag shape. No
      Chinese-developed AI model name appears anywhere in the file — no restricted
      model is used, configured, or referenced.
  - id: src/renderer/components/CommandPalette.tsx:a1850496108e365919ff906a3ad19aef03d419c820d362ae38782e0bd4fce0ad
    justification: >-
      False positive. This line is a code comment for the '#' launch mode
      describing how the shared seedPromptArgs helper delivers the opening prompt
      per harness (positional + '--' escape for claude/cursor/codex/pi, '--prompt'
      for OpenCode). No Chinese-developed AI model name appears anywhere in the
      file — no restricted model is used, configured, or referenced.

not_exploitable: []
