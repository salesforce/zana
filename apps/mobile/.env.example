# Optional for local builds. Use the UUID and owner of a Zana-owned EAS project.
EXPO_PUBLIC_EAS_PROJECT_ID=
EXPO_OWNER=

# Android Firebase client configuration, not a service-account private key.
# Locally: an absolute path to google-services.json. In EAS: a file environment variable.
GOOGLE_SERVICES_JSON=
