{
  "name": "@zana-ai/zcc-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Curated ZCC runtime bundle and local supervisor.",
  "files": ["dist", "README.md"],
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "source": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@zana-ai/zcc-contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^7.0.2"
  }
}
