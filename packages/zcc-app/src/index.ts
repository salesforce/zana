/**
 * The runtime bundle owns composition only. It packages built application
 * artifacts and later supervises the server and host daemon; it never imports
 * renderer or Electron source directly.
 */
export const ZCC_APP_RUNTIME_VERSION = 1;
