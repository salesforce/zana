/** Slice of product.ts — per-project settings / create-terminal. */

export type {
  ClaudeSettingsScope,
  ClaudeProjectFileId,
  ClaudeProjectSettings,
  ClaudeSettingsResult,
  CodexProjectSettings,
  CodexSettingsResult,
  OpenCodeProjectSettings,
  OpenCodeSettingsResult,
  ProjectSettings,
  CreateTerminalRequest
} from './product.js';
