/** Slice of product.ts — saved records / library vocabulary. */

export type {
  SavedDoc,
  SavedRecord,
  LibraryDocKind,
  LibraryScope,
  LibraryDoc,
  LibraryManifest,
  LibrarySearchHit,
  LibrarySearchResult,
  LibraryAddInput,
  SavedRecordInput
} from './product.js';
