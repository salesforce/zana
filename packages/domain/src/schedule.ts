/** Slice of product.ts — schedules and quick prompts. */

export type {
  ScheduleRun,
  ScheduleStatus,
  ScheduledTask,
  ScheduleCreateInput,
  ScheduleGroup,
  ScheduleGroupInput,
  ScheduleTemplate,
  QuickPrompt,
  ScheduleUpdateInput
} from './product.js';
