{
  "name": "@zcc/cli",
  "version": "0.1.0",
  "description": "Thin CLI for reading/authoring Zana Command Center stores",
  "license": "MIT",
  "type": "module",
  "bin": {
    "zcc": "./dist/bin/zcc.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/postbuild.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
