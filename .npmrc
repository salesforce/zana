registry=https://registry.npmjs.org/
# Electron rebuild currently resolves @electron/node-gyp from its upstream git
# repository. pnpm blocks exotic transitive dependencies by default; this
# explicit exception preserves the existing native-module build chain.
blockExoticSubdeps=false
