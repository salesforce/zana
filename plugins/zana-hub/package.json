{
  "name": "zana-hub",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Global Zana dashboard — a cross-project view of the Zana framework's teams, agents, sprints, and reusable catalog (profiles / teams / skills), read from ~/.zana.",
  "scripts": {
    "build:renderer": "BUILD_TARGET=renderer vite build",
    "build:main": "BUILD_TARGET=main vite build",
    "build": "npm run build:renderer && npm run build:main",
    "package": "node scripts/package.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "peerDependencies": {
    "react": "^18 || ^19"
  },
  "engines": {
    "zcc": ">=1.0.0",
    "zccPluginSdk": ">=0.1.0"
  },
  "zcc": {
    "name": "Zana",
    "description": "Orchestration dashboard for teams, sprints, tickets, and catalog",
    "branding": { "icon": "Workflow" },
    "server": "./dist/main.mjs",
    "app": "./dist/renderer.js"
  },
  "devDependencies": {
    "@zana-ai/zcc-extension-sdk": "^1.0.0",
    "react": "^19.2.8",
    "typescript": "^5.6.3",
    "vite": "^6.4.3"
  }
}
